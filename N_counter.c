#include <stdio.h>
#include <math.h>

int main(){
    /*
    int m1 = 1, int m2 = 2, int c1=74, int c2=10, int k0=27;
    int n1, n2;
    float N, k1;
    printf("%3s %3s\n", "N", "k1");
    for(n1=0;n1<=10;++n1){
        for(n2=0;n2<=10;++n2){
            N = (float)(m1 * (c1-c2) - c1*(m1-m2) + k0*(m1-m2))/(n1 * (m1-m2) - m1 * (n1-n2));
            k1 = (float) (n1*N+c1-k0) / m1;
            if (N > 0 && (floor(N) == N) && N < 128 && k1 < 128)
            printf("%3.of\n",N,k1);
        }
    }
    */
    printf("Hello");
    return 0;xx
}